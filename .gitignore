# secrets
.env
*.key

# python
__pycache__/
*.pyc
.pytest_cache/
.pytest-tmp/
.test-runs/
.coverage
htmlcov/
build/
dist/
*.egg-info/
.venv/
venv/

# runtime state (regenerated by `prooftrail env build`)
data/state/*.db
data/state/*.db-journal

# derived human-review aids (regenerate from immutable frozen sources)
evidence/review-pack-v1/

# NOTE: data/frozen/ and data/replay/ are COMMITTED on purpose — they are the
# frozen traces + cached LLM responses that make replay mode free for judges.
