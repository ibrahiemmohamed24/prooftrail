# Improvement Changelog

| Iteration | Date | Change | Evidence | Decision |
|---:|---|---|---|---|
| 0 | 2026-08-28 | Foundation schemas, identities, SQLite state, ledger, faults and ten families | 40 foundation tests | Continue: architecture viable, no end-to-end path yet |
| 1 | 2026-08-28 | Added atomic tools, deterministic cases and F02/F10 controls | Tool/fault integration tests | Keep independent ledger snapshots; timeout alone is not labelled harmful |
| 2 | 2026-08-28 | Added provider-neutral agent loop and explicit offline retry fixture | Trace proves two distinct calls under one intent | Keep real LLM outside deterministic smoke test; freeze it later |
| 3 | 2026-08-28 | Added hybrid auditor and evidence certificates | F02: `CONTRADICTED`, first bad event `#6` | Keep language extraction replaceable; keep reconciliation deterministic |
| 4 | 2026-08-28 | Added fair B1 contract, metrics, CLI and reproduction path | 98 tests; zero-key CLI demo | Do not publish headline delta until real frozen data and human labels exist |
| 5 | 2026-08-29 | Routed pytest temp files away from inaccessible `C:\\SADP_Temp` and disabled its unwritable cache | `python -m pytest` passed twice: 98/98 | Keep the default test command portable on managed Windows machines |
| 6 | 2026-08-29 | Live Anthropic provider behind `ModelClient` (model, prompt sha256, tokens, cost, stop reason, provider tool ids, verbatim thinking blocks), `BudgetGuard` over `data/replay/cost_ledger.jsonl`, bounded retry that never re-runs a tool, prompt-hash `ReplayCache`, `prooftrail agent run --live/--replay`, shared `run_case` runner; `anthropic` pinned to published version `1.2.0` | `python -m pytest`: 121/121, 94% coverage, all with a fake provider; `python -m prooftrail demo --json` unchanged (`CONTRADICTED`, event `#6`); `scripts/check_no_secrets.py` passed; `git diff --cached --check` clean | Keep the core stdlib-only (SDK imported lazily); no real-model claim until traces are frozen and replayed |
| 6b | 2026-08-29 | PR #1 review follow-up: budget guard reserves worst case for every retry attempt and counts every UTF-8 request byte as a token; unknown models are refused (`UnknownModelPricingError`) instead of priced by fallback; replay cache model mismatch is rejected before any API call (exit 5); atomic cache writes; `anthropic==1.2.0` pinned exactly; real-SDK tests over `httpx2.MockTransport` (request wire shape, typed responses, 529/429/500 retried, 400 not); README documents `--fresh` | `python -m pytest`: 129/129 (0 network); `python -m prooftrail demo --json` unchanged; `scripts/check_no_secrets.py` passed; `git diff --cached --check` clean; real spend $0.00 | Free plan: no live call is made until explicitly funded; the guard must be provably tighter than reality, never looser |
| 7 | 2026-08-29 | Added a no-billing Gemini Free Tier REST provider with real function calling, exact thought-signature circulation, quota pacing, bounded retry, provider-namespaced replay and explicit Free-plan confirmation | `python -m pytest --cov=prooftrail`: 130 passed, 94% total coverage; Gemini adapter 97%; real-call evidence still pending a user-owned Free Tier key | Use only synthetic data because Google may use Free Tier content to improve products; keep billed cost `$0` separate from list-price equivalent |
| 8 | 2026-08-29 | Validated the Free Tier route with a real Gemini run and changed the default to the responsive `gemini-3.1-flash-lite`; froze the first provider trace and replayed it without a network miss | `F02-s00`: 4 live LLM calls, 3,084 input + 422 output tokens, billed cost `$0`; replay returned 4 hits / 0 misses and the same `SUPPORTED` verdict; secret scan passed on trace and evidence | Keep the organic supported outcome; do not manufacture the scripted double-refund failure, and keep the milestone at 55 until all 40 traces exist |
| 9 | 2026-08-29 | Freeze tooling: `prooftrail freeze --live --case/--all`, `replay --all` (no key; exits non-zero on any divergence from the frozen bundle), `manifest` (40/40 inventory, totals, invariants: chains valid, labels provisional, auditor view family-blind, usage recorded, replay caches present), machine-checkable smoke checks, dataset tests that auto-enable once `data/frozen/` is populated; real-SDK wire test against a localhost stub | `python -m pytest`: 130 passed, 2 skipped (dataset tests wait for frozen cases), 94% coverage; `prooftrail manifest` correctly reports `0/40 (INCOMPLETE)` and exits 1 | Do not spend on 40 cases before a 3-case smoke run prints all `[ok ]`; no `verified_by_human: true` anywhere |
| 10 | 2026-08-29 | Merged the freeze runner and the PR #1 review follow-up into the Gemini branch; `freeze`/`replay`/`manifest` take `--provider gemini` (cache namespace `data/replay/gemini/`, no budget guard, `--budget-usd` rejected); resumable recording: cached turns are served first and the live client is built lazily only on a cache miss, so finished cases are never re-recorded and need no key; manifest records provider and list-price equivalent and enforces one model + one provider; smoke checks accept a recorded $0 cost | `F02-s00` frozen into `data/frozen/` from its real Gemini cache with 4 cached / 0 live calls, byte-identical to `evidence/runs/live/gemini/F02-s00/case.json`; `prooftrail replay --provider gemini --case F02-s00` ok; manifest `1/40`, all invariants yes; `python -m pytest`: 152 passed, 1 skipped (completeness waits for 40/40); secret scan passed; billed $0.00 | Remaining 39 cases run with the same `gemini-3.1-flash-lite` only; the manifest fails on any model mix |
| 11 | 2026-08-29 | Froze the complete 40-case dataset (10 families × 4 seeds) on the Gemini Free Tier with `freeze --live --provider gemini --all --skip-frozen`, resumed across quota/network pauses without re-recording any case; committed `data/frozen/` (40 bundles + manifest) and `data/replay/gemini/` (40 caches); README freeze snippet now shows the `--provider gemini` form used for the committed dataset; PROJECT_STATUS 55 → 75 | `data/frozen/manifest.json`: `40/40 cases frozen`, 175 LLM calls, 142,227 in / 16,950 out tokens, 133 tool calls, billed `$0.00` (list-price equivalent `$0.0610`), one model `gemini-3.1-flash-lite`, one provider, all 7 invariants `yes`, `problems: []`; `python -m prooftrail replay --provider gemini --all` with no key: `Replayed 40/40 cases with no API key; 0 failure(s)`; `python -m prooftrail manifest --provider gemini` exit 0; `python -m pytest`: 192 passed, 0 skipped, 94% coverage; `python -m prooftrail demo --json` unchanged (`CONTRADICTED`, event `#6`); `scripts/check_no_secrets.py` passed; `git diff --cached --check` clean | Verdicts 17 `SUPPORTED` / 16 `CONTRADICTED` / 7 `UNVERIFIABLE` are provisional and ledger-derived (`verified_by_human: false` everywhere); no benchmark number may be published from them; the 7 `UNVERIFIABLE` cases go to failure analysis, not re-rolls |
| 12 | 2026-08-29 | Follow-up before the PR: `sha256_file` hashes artifacts after CRLF-to-LF normalisation and every artifact writer (`write_case_artifacts`, `_dump`, `write_outputs`, `write_metrics`) passes an explicit LF `newline`, so `case_sha256`/`labels_sha256` are identical in a Windows working tree, in `git archive` and in a Linux clone; `data/frozen/manifest.json` regenerated (exactly the 80 hash lines changed, totals and invariants unchanged); stale texts fixed (README status 55%/139 tests/real traces pending, REPRODUCE Gemini cache path `data/replay/gemini/<case>.json`, CLI demo line `real-LLM dataset is still pending`) | Reproduced from `git archive 4fef244`: `test_committed_manifest_matches_the_files_on_disk` failed on `case_sha256` (hashes had been computed on CRLF). After the fix, from a `git archive` of the index tree (LF, 0 CRLF bytes): `python -m pytest` 194 passed (2 new tests: LF and CRLF hash identically; writers emit LF only), `prooftrail manifest --provider gemini` regenerates a byte-identical manifest, `replay --provider gemini --all` 40/40 with no key, secret scan passed, demo unchanged (`CONTRADICTED`, event `#6`); working tree: `manifest` then `git diff --exit-code` clean, 194 passed | Hash committed text artifacts on normalised bytes only, and never let the platform newline into anything that is hashed or committed |
| 13 | 2026-08-29 | Docs-only follow-up before the PR: README diagram now says `deterministic claim extraction (LLM-backed extractor planned)`; "Same evidence" states that the B1 contract and ProofTrail receive the same frozen trace + ledger while the B1 run and model/caps parity are pending; evaluation cases described as `provisional ledger-derived labels; human review pending`; status header says the two adapters use the same provider-namespaced replay mechanism; REPRODUCE honesty boundary records that the real agent run and the 40 traces are done and only human verification + the B1 comparison remain; `freeze` CLI help no longer claims it needs `ANTHROPIC_API_KEY` or spends money (selected-provider key on cache miss; Gemini Free Tier billed $0); dataset test also asserts every `labels_sha256` | `python -m pytest`: 194 passed (no behaviour change; one extra assertion); `git diff --check` clean; `scripts/check_no_secrets.py` passed | Keep every README/REPRODUCE claim tied to something that exists in the repository today; pending work stays labelled pending |
